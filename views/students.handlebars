<h3>{{student.first_name}} {{student.last_name}}</h3>

<h4>Equipment Checked Out:</h4>
{{#each equipments as |equipment|}}
<p>{{asset_name}}</p>
{{/each}}

<a href="/edit-student/{{student.id}}">Edit Student</a>
<button class="btn btn-primary">Delete Student</button>